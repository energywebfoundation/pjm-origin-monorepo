export enum TimeFrame {
    yearly,
    monthly,
    daily,
    weekly,
    hourly,
    halfHourly
}

export enum Currency {
    NONE,
    USD
}

export enum Compliance {
    none,
    REC
}

export enum Unit {
    kWh = 1e3,
    MWh = 1e6,
    GWh = 1e9
}